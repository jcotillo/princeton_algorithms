import fileinput

def min_priority_queue(transactions, file):
    pq = Priority_queue()
    with open(file) as f:
        # read line of f
        pq.insert()
        if pq.size() > M:
            pq.delMin()
